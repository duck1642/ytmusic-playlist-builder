auth_file: auth/oauth.json
oauth_client_file: auth/client_secret.json
artists_dir: artists
state_dir: state
cache_dir: cache
logs_dir: logs

playlist:
  privacy: PRIVATE
  max_tracks: 550
  update_mode: append_only

filters:
  exclude_live: false
  exclude_remix: false
  exclude_remaster: false
  exclude_deluxe: false
  exclude_karaoke: true
